{
  "rules": [
    {
      "allowed": { "origins": ["*"], "methods": ["GET", "HEAD"], "headers": ["Range", "If-Match", "If-None-Match"] },
      "exposeHeaders": ["ETag", "Content-Range", "Content-Length", "Accept-Ranges"],
      "maxAgeSeconds": 86400
    }
  ]
}
